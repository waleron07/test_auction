import type { RequestHandler } from 'msw';

/**
 * Обработчики четырёх операций контракта.
 * Наполняются в фазе 3 — сейчас каркас поднимается с пустым набором.
 */
export const handlers: RequestHandler[] = [];
