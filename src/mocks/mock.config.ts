/**
 * Задержка ответов моков, мс.
 *
 * Живёт в `src/mocks`, а не в `shared/config`: это свойство тестового дубля, и
 * прод-конфигу незачем знать ни о моках, ни тем более о том, что код запущен
 * под Vitest.
 *
 * В браузере задержка нужна, чтобы skeleton'ы и pending-состояния были видимы
 * при проверке. В тестах она бессмысленна — там никто не смотрит на скелетон, а
 * сотня запросов превращает секунды в десятки секунд.
 */
export const MOCK_DELAY_MS =
  import.meta.env.MODE === 'test'
    ? ({ min: 0, max: 0 } as const)
    : ({ min: 300, max: 600 } as const);
