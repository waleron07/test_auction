import { type SxProps, type Theme } from '@mui/material';

/**
 * Сетка списка аукционов: одна колонка на телефоне, две на планшете, три на
 * десктопе (карта ширин 0.7).
 *
 * Живёт отдельной константой, потому что её обязаны разделять список и его
 * скелетон: смысл скелетона в том, чтобы вёрстка не дёрнулась при появлении
 * данных, и раньше эта гарантия держалась на том, что два объекта `sx`
 * случайно совпадали.
 */
export const AUCTION_GRID_SX: SxProps<Theme> = {
  display: 'grid',
  gap: 2,
  gridTemplateColumns: {
    xs: '1fr',
    md: 'repeat(2, minmax(0, 1fr))',
    lg: 'repeat(3, minmax(0, 1fr))',
  },
};
