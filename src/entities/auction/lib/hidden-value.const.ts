/**
 * Текст вместо значения, скрытого организатором.
 *
 * Одна константа на весь слой: маппер `mapAuctionDetail` подставляет её вместо
 * значения, когда действует соответствующее разрешение (`permissions`), а
 * компонент `HiddenValue` по этому же значению решает, рисовать текст обычным
 * стилем или приглушённым. Две независимые копии строки дали бы либо опечатку
 * в сравнении, либо забытое место без приглушения.
 */
export const HIDDEN_BY_ORGANIZER = 'Скрыто организатором';
